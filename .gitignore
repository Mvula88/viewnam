# Local environment
.env
.env.local
.env.*.local

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.swo
~$*

# Word lockfiles + partial downloads
*.crdownload
*.tmp

# Dependencies (in case Node tooling lands later)
node_modules/

# Ad Inspiration is reference-only screenshots (subagent has already used them);
# kept locally but not deployed.
images/Ad Inspiration/

# Background Images IS tracked — they're needed for the deployed Design Studio.
# Source files are pre-compressed to ~1920px / 80% JPEG (~34 MB total) before
# committing so Vercel deploys stay well under tier limits.

# Working notes (not for production)
*.docx

# Local screenshots
Screenshot*.png

# Python generator artefacts
__pycache__/
*.pyc
content_extract.json

# Vercel local
.vercel/
